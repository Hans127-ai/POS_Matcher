1.	Хорошо получилось. Давай дополним графики для показателей 'kCAF', 'OSA', 'VISITS', 'PICOS', 'FACING', 'SHELF SHARE', 'MARKET SHARE'. В дополнение, к точкам понедельно, как это реализовано сейчас, нужно еще добавить линию которая покажет среднее значение для всех этих точек. Это среднее должно совпадать с тем значением, которое мы расчитали для подбора контрольных точек и записали в df_pair_list. Выбросы в расчёт среднего, конечно, не идут. Линия должна быть синего цвета (такая де как точки) и быть штриховой или штрих-пунктирной (т.е. не сплошной.)

2.	В ячейке 1.8 замени путь к папке с исходными данным по доле рынка. Новый путь 'C:\Users\rokotyev\Yandex.Disk_Main Data Rokotyan\2. Проекты\60. Подбор пар ТТ для тестов_data\marketshare\marketshare_csv'. Также немного поменялась структура данных, в них теперь нет поля CLIENT, а остались только поля YEAR_WEEK, SHIP_TO, MARKET_SHARE. На сколько я помню поле CLIENT мы не использовали в вычислениях, так что это ни на что не повлияет. Но перепроверь это.

3.	После нажатия кнопки "Рассчитать" в ячейке Ячейка номер: 4.1 система подвисает. Видимо ей нужно много времени на расчёты. Добавь пожалуйста индикатор прогресса справа от кнопки "Рассчитать" чтобы пользователь видел что приложение работает и не зависло. Мне кажется удобнее всего будет использовать виджет FloatProgress, но если это не так, то предложи мне свой вариант реализации.

4.	Нужно добавить перед ячейкой  2.1. добавить еще одну. В новой ячейке нужно создать виджет из двух горизонтально расположенных RadioButtons. Первая RadioButton будет называться "Подбор по всей АКБ", вторая RadioButton будет называться "Подбор по списку ТТ". Это выбор режима работы приложения. Если пользователь выбирает режим "Подбор по всей АКБ" то всё должно работать также как у нас реализовано сейчас. Но если пользователь выбирает режим "Подбор по списку ТТ" то это новая логика, которую тебе необходимо добавить в приложение. Я описываю её в пунктах ниже.

5.	В режиме "Подбор по списку ТТ" файл который мы загружаем в ячейке 2.1 содержит не список тестовых точек, а полный список точек среди которых приложению разрешается осуществить подбор. Далее в тексте промпта я буду называть этот список "полный список точек". 

6.	В режиме "Подбор по списку ТТ" выбор признаков из иерархии торговых , выбор недель, выбор KPI не меняются. Все должно работать также как в режиме "Подбор по всей АКБ". 

7.	Выбор количества точек в ячейке номер: 3.3 также не меняется в режиме "Подбор по списку ТТ" 

8.	Логику распределения точек из полного списка точек по группам нужно реализовать следующую. 
	* Алгоритм берёт первую торговую точку в полном списке торговых точек из числа тех, которым еще не присвоена группа. Эта точка считается тестовой и мы присваиваем ей признак в POS_TYPE ="Test_POS" (аналог тестовой точки в режиме )
	* Далее алгоритм действует также как в режиме "Подбор по всей АКБ", но выбор ведётся только среди точек из полного списка точек. 
	* Подобранные торговые точки в количестве заданном пользователем получают признак POS_TYPE ="Control_POS"
	* Заполнение df_pair_list для этих точек аналогично тому что мы реализовали в режиме "Подбор по всей АКБ"
	* Если не удалось найти контрольные точки в нужном количестве то действуем также как в режиме "Подбор по всей АКБ", т.е. добавляем строку POS_TYPE = "Control_POS_not_found"
	* Далее алгоритм ищет в полном списке следующую точку которой еще распределена в ту или иную группу и повторяет логику пока не распределенные точки в полном списке точек не закончатся.
	* Важно! Запрещено использовать одну и ту же точку в подборе больше одного раза. Другими словами одному SHIP_TO может быть присвоена только одна группа PAIR_NUMBER и только один тип POS_TYPE. 
	* Если тестовая точка не существует или тестовой точке не удалось подобрать ни одной контрольной точки из числа точек в полном списке точек, то логика такая же как в режиме "Подбор по всей АКБ"
	* Логика работы ячейки 4.2. и раздела 5 в режиме "Подбор по списку ТТ" не меняется
	* Обобщая всё выше сказанное. Логика нового режима очень похожа на ту, что мы реализовали для режима "Подбор по всей АКБ", но пространство для поиска пар в этом режиме значительно сужается и ограничивается только полным списком торговых точек. Также в режиме "Подбор по списку ТТ" мы не передаём список тестовых точек, алгоритм сам должен их определить.


--------------------------


1. Ячейка 2.0  расположил RadioButton вертикально, а нужно горизонтально.
2.  Функции cut_kpi_mean и  cut_kpi_trend в ячейке 3.1 работают медленно. Можно ли их как-то ускорить? Например, заменив итерирование на какие-то более быстрые подходы?
3.  На графики для  'kCAF', 'OSA', 'VISITS', 'PICOS', 'FACING', 'SHELF SHARE', 'MARKET SHARE',  которые мы выводим в разделе 5.3. нужно в дополнение к линии среднего добавить и значение этого среднего. Округлять его до двух знаков после запятой. 
4. На графики для  'SALES TREND. для MS TREND ,  которые мы выводим в разделе 5.3. нужно в дополнение к линии тренда добавить и угол этого тренда относительно горизонтальной оси.  Угол округлить до целых градусов.
