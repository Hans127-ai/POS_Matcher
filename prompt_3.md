1. Давай добавим Раздел №5 "Просмотр результатов". Добавить его нужно в самый конец файла.  Цель данного раздела - визуализировать пользователю данные по KPI по точкам которые попали в группы (пары), чтобы он смог визуально оценить качество подбора точек по тем параметрам, которые он задал ранее, перед расчётом.
2. В начале раздела сделай текстовый заголовок как в других разделах
3. Затем должен идти виджет с выпадающим списком Dropdown. Список содержит все пары из df_pair_list которые мы записали в столбце PAIR_NUMBER. Выбрать можно только одну пару из списка. 
4. После выбора пары ниже нужно вывести графики для всех KPI которые выбрал пользователь в ячейке 2.4. 
5. Графики нужно расположить по следующим правилам: 
	  * Графики для одного и того же KPI должны быть расположены в одну линию (строку)
	  * Слева от каждой такой линии надо вывести название KPI
	  * Внутри линии самый первый график должен быть построен по данным для тестовой точки, остальные графики это графики по данным контрольных точек
	  * Соответсвтенно, если в ячейке 2.4 пользователь выбрал 2 KPI: OSA и PICOS, а в ячейке 3.3 он выбрал поиск двух контрольных точек для каждой тестовой, то мы должны увидеть две строки (OSA и PICOS) с тремя графиками в каждой (первый для тестовой точки в выбранной паре, а остальные 2 графика для данных контрольных точек которые удалось подобрать)

6. Графики для показателей 'kCAF', 'OSA', 'VISITS', 'PICOS', 'FACING', 'SHELF SHARE', 'MARKET SHARE' должны представлять собой  Scatterplot с  белым фоном и точками синего цвета. По горизонтальной оси нужно отложить недели, по вертикальной оси значение KPI для данной недели.
7. Выборка недель должна соответствовать той что была использована при расчете KPI для подбора тестовых точек. Другими словами, она должна быть отфильтрована так как пользователь указал в ячейке  2.3.
8. Построенный на данных из датафреймов служащих основной для расчета данных показателей при подборе пар  df_sales, df_osa, df_visits, df_facing_fact, df_picos, df_shelfshare, df_marketshare.
9. Значения KPI которые мы посчитали выбросом при расчете показателей для подбора тестовых точек, необходимо оставить на графике. Но точки построенные по таким данным (т.е. выбросы) должны быть красного цвета.
10. При нажатии на график должен происходить его увеличение (zoom) если такая функция есть в seaborn или matplotlib и это можно легко релизовать. Сложных решений не нужно, лучше я пока откажусь от этого требования.
11. Для показателей KPI 'SALES TREND', 'MS TREND' внешний вид каждого графика будет немного отличаться. Необходимо построить Scatterplot для базовых показателей, а поверх него наложить приямую линию тренда. Т.е для SALES TREND' мы строим  Scatterplot для 'kCAF', и поверх него рисуем линию тренда SALES TREND. для MS TREND мы строим  Scatterplot для 'MARKET SHARE', и поверх него рисуем линию тренда SMS TREND. Все что выше я написал про выборку недель, цвета, вёрстку раздела и выбросы актуально и для данного пункта.
12. Если что-то непонятно уточни у меня реализацию.
13. При пересчёте df_pair_list, т.е. при нажатии кнопки  'Рассчитать' в ячейке 4.1 графики долдны или пропадать с экрана до выбора пары или автоматически пересчитываться.
14. Помни что графики будут отображаться на экране ноутбука 14 дюймов. Вёрстка должна соответствовать этому экрану.

